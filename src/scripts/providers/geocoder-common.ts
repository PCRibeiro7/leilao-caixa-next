export type Coordinates = {
    latitude1: number;
    longitude1: number;
    latitude2: number;
    longitude2: number;
};

export const randomUserAgent = `Leilao-Caixa-App-${Math.random().toString(36).substring(7)}`;
